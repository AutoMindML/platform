# AutoMind Web Frontend

## Setup

```bash
pnpm install
pnpm run db-gen

# if pnpm install stuck, try following commands:

# for powershell only
Set-ExecutionPolicy Remotesigned -Scope CurrentUser -Force | Out-Null

pnpm store prune
pnpm install
```
